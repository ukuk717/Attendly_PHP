<IfModule mod_rewrite.c>
  RewriteEngine On

  # Route all non-file requests to Slim front controller
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>

